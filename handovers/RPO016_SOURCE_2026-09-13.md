# RPO-016 blocked preservation plans

Source checkpoint only. No version bump or new ZIP in this commit.

When the host returns blocked_preservation_state, CleanupPlanService records a
blocked audit outcome, CleanerPersistenceService persists status blocked and the
complete plan, and the existing plan page shows the table/reason and withholds
the purge form even when a backup exists. This does not create a module-owned
destructive path or replace the host's execution-time preservation check.

Proof against the changed source and actual host:
- Repository harness: 6/6 passed; zero skipped.
- Host ModuleCleanerBackupRestoreTest named held-evidence HTTP test: 1 test,
  13 assertions passed. Actual persisted plan readback and authenticated GET,
  normal middleware, table/reason displayed, execute action absent.
- Existing backup/restore behavior passed in the collect-all source test run.

The host test defaults to the current published shelf; the source check used
LONGLINK_MODULE_CLEANER_SOURCE explicitly. It is not packaged acceptance.
The next grouped host must contain RPO-016 before the dependent Cleaner ZIP is
published. Keep the published version unchanged until that package is built.
No RPO or programme identifier closes here.
